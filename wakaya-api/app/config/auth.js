let auth = {}

const SEED = 'algo-muy-dificl-de-adivinar-wakaya'

const GoogleAuth = {
    service: 'Gmail',
    auth: {
        user: "manumayo8@gmail.com",
        pass: "loper102"
    }
}

auth.SEED = SEED
auth.GoogleAuth = GoogleAuth

module.exports = auth